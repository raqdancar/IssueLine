{
  "installCommand": "npm --prefix ui install --include=optional",
  "buildCommand": "npm run build",
  "outputDirectory": "ui/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
