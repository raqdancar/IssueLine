{
  "installCommand": "npm --prefix ui install --include=optional --no-package-lock",
  "buildCommand": "npm run build",
  "outputDirectory": "ui/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
